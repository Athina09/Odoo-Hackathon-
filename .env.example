# EcoSphere — optional env for full dev stack (npm run dev:all)
# Copy: cp .env.example .env

# Backend API port (default 8000)
API_PORT=8000

# Streamlit RAG dashboard port (default 8501)
RAG_DASHBOARD_PORT=8501
